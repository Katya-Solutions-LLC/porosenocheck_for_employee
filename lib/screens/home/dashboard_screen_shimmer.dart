import 'package:flutter/material.dart';

class DashboardScreenShimmer extends StatelessWidget {
  const DashboardScreenShimmer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
