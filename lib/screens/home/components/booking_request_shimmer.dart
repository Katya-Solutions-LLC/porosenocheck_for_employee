import 'package:flutter/material.dart';

class BookingRequestShimmer extends StatelessWidget {
  const BookingRequestShimmer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
