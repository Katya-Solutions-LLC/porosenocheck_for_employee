import 'package:flutter/material.dart';

class UpcomingBookingShimmer extends StatelessWidget {
  const UpcomingBookingShimmer({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
